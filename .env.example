# Local reference only. Configure this secret in the EdgeOne Makers project
# environment settings; do not commit a populated .env file.
OPENAI_API_KEY=
# Optional. Keep false/unset unless an authenticated gateway supplies keys.
ALLOW_REQUEST_API_KEY=false
MODEL_GATEWAY_URL=https://api.openai.com/v1/chat/completions
MODEL_NAME=gpt-4o-mini
# Optional client authentication token for the Agent endpoint.
AGENT_AUTH_TOKEN=
